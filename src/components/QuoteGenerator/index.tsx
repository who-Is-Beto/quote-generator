import { FC, ReactNode, ComponentProps } from "react";

const QuoteGenerator: FC = (): ReactNode => {
  return (
    <main className="min-h-screen min-w-fit bg-cover bg-gradient-to-r from-slate-900 to-cyan-500 animate-gradient">
      QuoteGenerator
    </main>
  );
};

export default QuoteGenerator;
